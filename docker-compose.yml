services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      DB_HOST: ${DB_HOST:-db}
      DB_NAME: ${DB_NAME:-lanotte}
      DB_USER: ${DB_USER:-lanotte}
      DB_PASS: ${DB_PASS:-lanotte123}
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-lanotte}
      MYSQL_USER: ${MYSQL_USER:-lanotte}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-lanotte123}
    healthcheck:
      test:
        - CMD-SHELL
        - mysqladmin ping -h localhost -u root -p$$MYSQL_ROOT_PASSWORD --silent
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - ./banco.sql:/docker-entrypoint-initdb.d/init.sql:ro